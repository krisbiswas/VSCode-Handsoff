name: unified_audio
description: A 4-screen Flutter app with MVVM, navigation, and custom themes.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  # shared_preferences: ^2.1.1
  # http: ^0.15.0
  # path_provider: ^2.0.11
  # permission_handler: ^10.2.0
  # audioplayers: ^0.20.1
  get: ^4.7.2
  # flutter_colorpicker: ^1.0.3
  flutter_reactive_ble: ^5.4.0
  logger: ^2.6.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    # - assets/
